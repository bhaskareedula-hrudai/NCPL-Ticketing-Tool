# Root-level requirements.txt — used by Vercel's @vercel/python runtime
# when deploying the backend as a serverless function via /api/index.py.

fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pydantic>=2.6.4
requests>=2.31.0
python-multipart>=0.0.9
pg8000>=1.30.0
